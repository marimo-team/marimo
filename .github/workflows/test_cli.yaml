# This workflow has been temporarily simplified to focus on Windows LSP copilot path testing
# The main Windows LSP test has been moved to test_be.yaml

name: cli test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder job
        run: echo "CLI tests moved to test_be.yaml for Windows LSP copilot path testing"

