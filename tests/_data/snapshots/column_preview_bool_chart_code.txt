_chart = (
    alt.Chart(table)
    .mark_bar()
    .encode(
        x=alt.X("bool_col", type="nominal"),
        y=alt.Y("count()", type="quantitative"),
        tooltip=[
            alt.Tooltip("bool_col", type="nominal"),
            alt.Tooltip(
                "count()",
                type="quantitative",
                format=",.0f",
            ),
        ],
    )
    .properties(width="container")
)
_chart